<Window x:Class="control.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:control"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <DataGrid ItemsSource="{Binding list}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Имя" Binding="{Binding Path=Name}" Width="90" />
                <DataGridTextColumn Header="Комиссионные" Binding="{Binding Path=Commissions}" Width="100" />
                <DataGridTextColumn Header="Дневная выручка" Binding="{Binding Path=Revenue}" Width="110" />
                <DataGridTextColumn Header="Стаж работы" Binding="{Binding Path=Seniority}" Width="90" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
